{
  "name": "praxis-bestellungen-functions",
  "version": "1.0.0",
  "description": "Backend functions for the order management app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "web-push": "^3.6.6"
  }
}